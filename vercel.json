{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static"
    }
  ],
  "outputDirectory": "dist",
  "public": true,
  "buildCommand": "npm run build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
